# Resend — https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxx

# Where contact form emails are delivered
CONTACT_TO_EMAIL=kube8eliyahu@gmail.com

# Sender identity — must be a verified domain in Resend (or use onboarding@resend.dev for testing only)
RESEND_FROM=Kube Eliyahu <onboarding@resend.dev>

# Optional: only if the React app is on a different origin than the API (full URL to POST endpoint)
# VITE_CONTACT_API_URL=https://your-project.vercel.app/api/contact
